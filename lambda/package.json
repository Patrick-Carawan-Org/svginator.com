{
  "name": "svginator-generator",
  "version": "1.0.0",
  "description": "Lambda function to generate SVG files from base 64 encoded strings",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "lambda-local -l index.js -W 8080",
	"zip": "zip -r lambda.zip index.js package.json node_modules"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "base64url": "^3.0.1",
    "lambda-local": "^2.2.0"
  }
}
